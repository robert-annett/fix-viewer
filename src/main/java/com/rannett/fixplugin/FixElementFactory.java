package com.rannett.fixplugin;

public class FixElementFactory {

//    public static FixValue createValue(Project project, String valueText) {
//        String dummyText = "10=" + valueText + "\u0001"; // Valid FIX message
//        PsiFile dummyFile = PsiFileFactory.getInstance(project)
//                .createFileFromText("dummy.fix", FixFileType.INSTANCE, dummyText);
//
//        if (!(dummyFile instanceof FixFile)) return null;
//
//        FixField field = PsiTreeUtil.findChildOfType(dummyFile, FixField.class);
//        return field != null ? field.getValue() : null;
//    }
}
